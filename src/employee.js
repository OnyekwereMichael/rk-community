
import propTypes from "prop-types";

function Employee  (props)  {

  
//   const  { title, age } = props;
//   const { name } = props;
//   const { id } = props;
//   const { student} = props;
  
//   const {item} = props;
      //   return (
      //   <div>
          
      //      <p>{title} {age}</p>
      //      <p>{item}</p>

      //      <p>Name: {name}</p>
      //      <p>do you know that i am : {id} years old?</p>
      //      <p>i am a currently a: {student ? "yes" : "no"}</p>
        
      //   </div>


      //   );
      }
      // Employee.defaultProps= {
      // name: "guest",
      // id: 20,
      // student: "false",
      //       }

      //       Employee.propTypes = {
      //           name: propTypes.string
      //       }
        
         

            {/* <div>
              <p>{"the first name is"} {name}</p>
          <button onClick={() =>{
              setName("ilogbo")
              
          }}>Change name</button>
          
          <button onClick={() =>{
              setName("Michael")
            }}>return name </button>
              </div>
          
          
            
          
          
              <main>
                <p style={{background: "red"}}>redmi</p>
              </main> */}

      

{/* 

const food = [
   {
    name: "joe",
    id: 20
   }, 
   {
    name: "joe",
    id: 50
   }, 
   {
    name: "joe",
    id: 34
   } 
  ]

  const yes = () =>{
    const girl = ["bob", "mary", "jane"];
    const int = Math.floor(Math.random() * 3)
    return girl[int];
  }

  const style = {
    position: "absolute",
    left: "10%",
    display: "grid",
    gridtemplatecolumn: "1fr 1fr 1fr",
    backgroundColor: "red",
  } */}


{/* 
food.map(item =>(
    <div>
    <p>who do you want to  be your partner, is it {yes()}</p>
    <p> nane is mike {item.name} </p>
    <h6 style = {style}>chike the boss {item.id}</h6>
    <h5>kuyku {item.name}</h5>

  </div> */}



{/* const [count, setCount] = useState(0)

const increase = () =>{
  setCount(count + 1)
}
    return (
      <div>
        <p>This is the first{count}</p>

        <button onClick={increase}>+</button>
     <button onClick={() =>{
      setCount(count - 1)
     }}>-</button>
    </div>

 
    
    );
  } */}
   
    
      export default Employee;